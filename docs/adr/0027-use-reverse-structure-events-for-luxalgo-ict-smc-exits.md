# Use reverse structure events for LuxAlgo ICT/SMC exits

LuxAlgo ICT/SMC candidate strategies use reverse Structure Events as the primary exit signal instead of fixed take-profit exits. Stop loss remains a protective risk control and is checked before reverse-event exits, while intraday flat still closes any remaining position before the RTH session ends.
