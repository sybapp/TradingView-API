# Use continuous futures with explicit roll metadata first

The first ES dataset may use a TradingView continuous contract for signal research, but the dataset must explicitly record that it is continuous futures data and capture roll policy metadata. This lets the first loop move quickly while preserving the distinction between convenient research data and directly tradable single-contract futures data.
