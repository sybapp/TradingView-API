# Limit the LLM to candidate proposal and analysis

The LLM will act as a candidate proposer and analyst rather than an unrestricted optimizer or strategy executor. Any LLM-suggested idea must be represented as a validated strategy spec and scored by NautilusTrader walk-forward validation before it can affect rankings.
