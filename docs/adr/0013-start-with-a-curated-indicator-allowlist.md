# Start with a curated indicator allowlist

The first strategy search will use a curated allowlist of TradingView indicators rather than every available public, private, or invite-only indicator. A bounded indicator universe keeps feature extraction, overfitting control, dataset versioning, and strategy comparison tractable while the validation loop is being built.
