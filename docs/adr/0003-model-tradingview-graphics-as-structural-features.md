# Model TradingView graphics as structural features

TradingView indicator graphics will be normalized into typed structural features instead of treated as screenshots or images. This keeps the strategy loop interpretable, aligns graphics with bars and timestamps, and lets NautilusTrader consume indicator-derived events, zones, lines, and profiles as custom data.
